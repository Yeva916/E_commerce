[project]
name = "contracts"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Yeshwant Kuriyavar", email = "yeshwa916@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
    "email-validator>=2.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/contracts"]
